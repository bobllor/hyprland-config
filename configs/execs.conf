# wallpaper
exec-once = swww-daemon

# notifications
exec-once = swaync

# hyprland stuff
exec-once = waybar
exec-once = hypridle

#exec-once=/usr/lib/polkit-kde/authentication-agent-1 # i actually do not know what this is. i have to read on it.

# clipboards
exec-once = wl-paste --type text --watch cliphist store
exec-once = wl-paste --type image --watch cliphist store
